{
  "model_name_or_path": "microsoft/trocr-large-handwritten",
  "output_dir": "checkpoints/trocr-large-400k",
  "device": "mps",
  "mixed_precision": "fp16",
  "gradient_checkpointing": true,
  "batch_size": 32,
  "eval_batch_size": 32,
  "num_workers": 0,
  "gradient_accumulation_steps": 2,
  "learning_rate": 5e-5,
  "weight_decay": 0.01,
  "num_train_epochs": 3,
  "warmup_ratio": 0.03,
  "lr_scheduler_type": "cosine",
  "logging_steps": 5,
  "eval_steps": 500,
  "save_steps": 500,
  "save_total_limit": 3,
  "empty_cache_steps": 50
}
